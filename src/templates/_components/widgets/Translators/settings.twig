{% import "_includes/forms" as forms %}

{{ forms.hidden({
    label: "Limit"|t('app'),
    id: 'limit',
    name: 'limit',
    value: widget.limit,
    size: 2,
    errors: widget.getErrors('limit')
}) }}