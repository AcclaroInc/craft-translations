{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: "Limit"|t('app'),
    id: 'limit',
    name: 'limit',
    value: widget.limit,
    size: 2,
    errors: widget.getErrors('limit')
}) }}

{{

forms.selectField({
    label: "Date Range"|t('app'),
    id: 'days',
    name: 'days',
    value: widget.days,
    options: options,
    errors: widget.getErrors('days')
})
}}